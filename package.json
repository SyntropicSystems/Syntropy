{
  "name": "syntropy",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "build": "nx run-many -t build",
    "test": "nx run-many -t test",
    "lint": "nx run-many -t lint",
    "dev:web": "nx run web:dev",
    "dev:mobile": "nx run mobile:dev",
    "dev:dev-web": "nx run dev-web:dev",
    "deploy:functions": "nx run functions:deploy"
  },
  "devDependencies": {
    "nx": "^20.4.0",
    "typescript": "^5.7.0",
    "@nx/js": "^20.4.0"
  }
}
