<!-- syntropy:generated -->
# Syntropy CLI Reference

Generated by `syntropy gen cli-docs`.

## `syntropy`

```text
Syntropy workspace platform CLI (bootstrap slice)

Usage: syntropy [OPTIONS] <COMMAND>

Commands:
  init      Initialize a workspace (creates syntropy.toml if missing)
  tree      Render a directory tree (human or JSON)
  info      Describe a path (purpose, rules, boundaries)
  gen       Generate/refresh deterministic workspace artifacts
  validate  Validate workspace structure against the selected blueprint
  check     Run all drift gates and workspace validation
  help      Print this message or the help of the given subcommand(s)

Options:
      --json
          Emit machine-readable JSON output

  -h, --help
          Print help
```

## `syntropy check`

```text
Run all drift gates and workspace validation

Usage: syntropy check

Options:
  -h, --help
          Print help
```

## `syntropy gen`

```text
Generate/refresh deterministic workspace artifacts

Usage: syntropy gen <COMMAND>

Commands:
  readmes   Generate README.md files for known blueprint directories
  agents    Generate/refresh `.claude/**` and `.codex/**` adapters from canonical specs
  cli-docs  Generate/refresh CLI reference documentation
  all       Run all generators (CLI docs, README contracts, agent adapters)
  help      Print this message or the help of the given subcommand(s)

Options:
  -h, --help
          Print help
```

## `syntropy gen agents`

```text
Generate/refresh `.claude/**` and `.codex/**` adapters from canonical specs

Usage: syntropy gen agents [OPTIONS]

Options:
      --dry-run
          Print what would change, but do not write

      --check
          Fail if adapter generation would make changes (drift gate)

  -h, --help
          Print help
```

## `syntropy gen all`

```text
Run all generators (CLI docs, README contracts, agent adapters)

Usage: syntropy gen all [OPTIONS]

Options:
      --dry-run
          Print what would change, but do not write

      --check
          Fail if generation would make changes (drift gate)

  -h, --help
          Print help
```

## `syntropy gen cli-docs`

```text
Generate/refresh CLI reference documentation

Usage: syntropy gen cli-docs [OPTIONS]

Options:
      --dry-run
          Print what would change, but do not write

      --check
          Fail if CLI doc generation would make changes (drift gate)

  -h, --help
          Print help
```

## `syntropy gen readmes`

```text
Generate README.md files for known blueprint directories

Usage: syntropy gen readmes [OPTIONS]

Options:
      --dry-run
          Print what would change, but do not write

      --check
          Fail if README generation would make changes (drift gate)

  -h, --help
          Print help
```

## `syntropy info`

```text
Describe a path (purpose, rules, boundaries)

Usage: syntropy info <PATH>

Arguments:
  <PATH>
          Path to describe

Options:
  -h, --help
          Print help
```

## `syntropy init`

```text
Initialize a workspace (creates syntropy.toml if missing)

Usage: syntropy init [OPTIONS] [PATH]

Arguments:
  [PATH]
          Workspace root (defaults to current directory)
          
          [default: .]

Options:
      --name <NAME>
          Workspace name (defaults to directory name)

      --blueprint <BLUEPRINT>
          Blueprint ID (default: north-star/v0)

  -h, --help
          Print help
```

## `syntropy tree`

```text
Render a directory tree (human or JSON)

Usage: syntropy tree [OPTIONS] [PATH]

Arguments:
  [PATH]
          Path to render (defaults to workspace root)
          
          [default: .]

Options:
      --depth <DEPTH>
          Max depth (0 = only the node itself)
          
          [default: 4]

  -h, --help
          Print help
```

## `syntropy validate`

```text
Validate workspace structure against the selected blueprint

Usage: syntropy validate

Options:
  -h, --help
          Print help
```

