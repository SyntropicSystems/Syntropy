{
  "name": "@syntropy/functions",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo \"Functions build — not yet configured\"",
    "deploy": "echo \"Functions deploy — not yet configured\"",
    "test": "echo \"No tests yet\"",
    "lint": "echo \"No linter configured yet\""
  },
  "dependencies": {
    "@syntropy/domain-core": "workspace:*",
    "@syntropy/domain-events": "workspace:*",
    "@syntropy/firebase": "workspace:*",
    "@syntropy/shared-types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
