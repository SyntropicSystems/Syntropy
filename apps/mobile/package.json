{
  "name": "@syntropy/mobile",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "echo \"Expo dev — not yet configured\"",
    "build": "echo \"Expo build — not yet configured\"",
    "test": "echo \"No tests yet\"",
    "lint": "echo \"No linter configured yet\""
  },
  "dependencies": {
    "@syntropy/domain-core": "workspace:*",
    "@syntropy/domain-events": "workspace:*",
    "@syntropy/firebase": "workspace:*",
    "@syntropy/shared-types": "workspace:*",
    "@syntropy/design-tokens": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
