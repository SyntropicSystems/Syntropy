/**
 * @syntropy/domain-core
 *
 * Unified domain logic package (Phase 1). Contains all domain models,
 * business rules, and Firestore operations in one place.
 *
 * Will be split into domain-tasks, domain-projects, domain-queue,
 * domain-spaces, domain-artifacts, domain-integrations, domain-ai
 * as individual domains grow complex enough to warrant separation.
 */

export * from "./task.js";
