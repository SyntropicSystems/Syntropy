/**
 * @syntropy/firebase
 *
 * Firebase client configuration, Firestore helpers, auth utilities,
 * and Cloud Storage wrappers. Shared by all apps and Cloud Functions.
 *
 * Firebase SDK dependencies will be added when implementation begins.
 */

export {};
