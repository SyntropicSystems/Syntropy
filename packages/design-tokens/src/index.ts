/**
 * @syntropy/design-tokens
 *
 * Platform-agnostic design tokens: colors, spacing, typography, shadows.
 * Consumed by both ui-web and ui-mobile packages.
 *
 * These are plain JS values, not CSS — each UI package transforms
 * them into the appropriate format (CSS-in-JS, StyleSheet, etc.).
 */

export {};
