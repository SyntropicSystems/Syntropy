/**
 * Syntropy Infrastructure
 *
 * Pulumi program for managing Firebase and GCP resources.
 * Stack configuration (project ID, region, etc.) comes from
 * Pulumi.<stack-name>.yaml files, not hardcoded here.
 */

export {};
