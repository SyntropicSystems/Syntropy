# System Workflows

This folder contains executable “how-to” docs for system-of-work maintenance.

- Start with `agents-sync.md` when changing canonical agent specs.
- Use `create-pr.md` to turn changes into a reviewable PR (branch, validation, commit, PR).
